#include <stdio.h>

    // Aula sobre a estrutura de repetição While.

int main(){
    /*
    int x = 100;
    while(x <= 200){
        if (x % 2 == 0){
            printf("%d\n", x);
        }
        x++;
    }
    */

    /*
    int ct = 0;
    int x = 0;
    int y = 0;
    printf("Informe o primeiro valor: ");
    scanf("%d", &x);
    printf("\nInforme o segundo valor: ");
    scanf("%d", &y);
    while (x <= y){
        if(x % 2 == 0 ){
            ct++;
        }
        x++;
    }
    printf("O pares entre os valores: %d", ct);
    */

    /*
    int soma = 0;
    int x = 0;
    int y = 0;
    printf("Informe o primeiro valor: ");
    scanf("%d", &x);
    printf("\nInforme o segundo valor: ");
    scanf("%d", &y);
    while (x <= y){
        if(x % 2 == 0 ){
            soma += x;
        }
        x++;
    }
    printf("A soma dos valores: %d", soma);
    */

}
