#include <stdio.h>
#include <math.h>

int r = 0;
volume = 0;

int main(){
    printf("Faça o programa que calcule o volume de uma esfera de raio R. O usuário fornecerá o dado necessário. \nOnde: volume = 4.r3/3");
    printf("\nInforme o valor do raio da esfera: ");
    scanf("%d", &r);
    volume = 4/3 * (pow(r, 3));
    printf("%d", volume);
}
W
